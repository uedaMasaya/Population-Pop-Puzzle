<h2>プロフィール編集</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <div class="field" style="margin-bottom: 30px;">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>

  <div class="field" style="margin-bottom: 30px;">
    <%= f.label :email %><br />
    <%= f.email_field :email %>
  </div>

  <div class="field" style="margin-bottom: 0px;">
    <%= f.label :password %> <i>(変更する場合のみ入力してください)</i><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field" style="margin-bottom: 40px;">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="field" style="margin-bottom: 20px;">
    <%= f.label :current_password %> <i>(変更を保存するには現在のパスワードを入力してください)</i><br />
    <%= f.password_field :current_password, autocomplete: "off" %>
  </div>

  <div class="actions" style="margin-bottom: 40px;">
    <%= f.submit "更新" %>
  </div>
<% end %>

<h3>アカウント削除</h3>

<p>アカウントを削除すると、すべてのデータが永久に削除されます。</p>

<%= button_to "アカウント削除", registration_path(resource_name), method: :delete, data: { confirm: "本当にアカウントを削除しますか？" } %>